---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "datahub_custom_assertion Resource - datahub"
subcategory: ""
description: |-
  DataHub ✅ | DataHub Cloud ✅
  Creates and manages a custom (external) DataHub assertion.
  Custom assertions are evaluated by an external system (e.g. dbt tests, Great Expectations, a custom script) and reported back to DataHub via the reportAssertionResult API. This resource declares the assertion definition and associates it with a dataset; it does not run the assertion itself.
  URN
  DataHub generates a server-side UUID for each assertion. The urn and id attributes are populated after creation and are stable across updates. ImportState requires the full assertion URN (e.g. urn:li:assertion:<uuid>).
---

# datahub_custom_assertion (Resource)

**DataHub ✅ | DataHub Cloud ✅**

Creates and manages a custom (external) DataHub assertion.

Custom assertions are evaluated by an external system (e.g. dbt tests, Great Expectations, a custom script) and reported back to DataHub via the `reportAssertionResult` API. This resource declares the assertion definition and associates it with a dataset; it does not run the assertion itself.

## URN

DataHub generates a server-side UUID for each assertion. The `urn` and `id` attributes are populated after creation and are stable across updates. ImportState requires the full assertion URN (e.g. `urn:li:assertion:<uuid>`).



<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `assertion_type` (String) Type label for the assertion (e.g. `CUSTOM`, `FRESHNESS`, `VOLUME`). Arbitrary string used for display and filtering in DataHub.
- `description` (String) Human-readable description of what this assertion checks.
- `entity_urn` (String) URN of the DataHub dataset this assertion monitors.
- `platform_urn` (String) URN of the data platform that evaluates this assertion (e.g. `urn:li:dataPlatform:dbt`).

### Optional

- `external_url` (String) URL linking to the assertion definition in an external system.
- `field_path` (String) Column or field path this assertion relates to. Omit for table-level assertions.
- `logic` (String) Logic description or expression for the assertion.
- `on_failure_actions` (List of String) Actions to take when the assertion fails (e.g. `["RAISE_INCIDENT"]`).
- `on_success_actions` (List of String) Actions to take when the assertion passes (e.g. `["RESOLVE_INCIDENT"]`).

### Read-Only

- `id` (String) The ID of this resource.
- `tags_all` (Set of String) Tag URNs attached by the provider's `defaults.tags`. While non-null, the provider owns the complete `globalTags` list on this entity: tags added outside Terraform show as drift here and are removed on the next apply. Null when `defaults.tags` is not configured and the provider has never written tags to this entity (existing and externally-applied tags are then left untouched).
- `urn` (String) Full DataHub URN for this assertion (e.g. `urn:li:assertion:<uuid>`).
