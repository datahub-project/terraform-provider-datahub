---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "datahub_structured_properties Data Source - datahub"
subcategory: ""
description: |-
  DataHub ✅ | DataHub Cloud ✅
  Returns the URNs of all DataHub structured property definitions visible to the authenticated principal.
  Backed by searchAcrossEntities (OpenSearch). Properties created within the last few seconds may not yet appear. Use the returned urns list as the for_each value in import {} blocks to bulk-import existing structured properties into Terraform state.
---

# datahub_structured_properties (Data Source)

**DataHub ✅ | DataHub Cloud ✅**

Returns the URNs of all DataHub structured property definitions visible to the authenticated principal.

Backed by `searchAcrossEntities` (OpenSearch). Properties created within the last few seconds may not yet appear. Use the returned `urns` list as the `for_each` value in `import {}` blocks to bulk-import existing structured properties into Terraform state.

## Example Usage

```terraform
# List all structured properties URNs, e.g. to bulk-import them into Terraform state.
data "datahub_structured_properties" "all" {}

output "structured_property_urns" {
  value = data.datahub_structured_properties.all.urns
}

# Feed the urns into an import {} for-each block to adopt existing structured properties
# (requires Terraform >= 1.11 for import for_each):
#
# import {
#   for_each = toset(data.datahub_structured_properties.all.urns)
#   to       = datahub_structured_property.imported[each.value]
#   id       = each.value
# }
```

<!-- schema generated by tfplugindocs -->
## Schema

### Read-Only

- `urns` (List of String) List of DataHub URNs for all structured properties.
