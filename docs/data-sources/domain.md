---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "datahub_domain Data Source - datahub"
subcategory: ""
description: |-
  DataHub ✅ | DataHub Cloud ✅
  Looks up an existing DataHub domain by domain_id.
  Use this to reference a domain that already exists in DataHub -- for example one created in the DataHub UI or via the Python SDK -- as a parent_domain in a datahub_domain resource without taking ownership of it.
---

# datahub_domain (Data Source)

**DataHub ✅ | DataHub Cloud ✅**

Looks up an existing DataHub domain by `domain_id`.

Use this to reference a domain that already exists in DataHub -- for example one created in the DataHub UI or via the Python SDK -- as a `parent_domain` in a `datahub_domain` resource without taking ownership of it.

## Example Usage

```terraform
data "datahub_domain" "finance" {
  domain_id = "finance"
}

output "finance_urn" {
  description = "URN of the Finance domain."
  value       = data.datahub_domain.finance.urn
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `domain_id` (String) The unique identifier of the domain to look up (the URN suffix, e.g. `marketing`).

### Read-Only

- `custom_properties` (Map of String) Arbitrary key-value metadata attached to the domain. Null if none are set.
- `description` (String) Description of the domain. Empty if not set.
- `name` (String) Human-readable display name of the domain.
- `parent_domain` (String) Full URN of the parent domain, or empty if this is a root domain.
- `urn` (String) Full DataHub URN for this domain (e.g. `urn:li:domain:marketing`).
